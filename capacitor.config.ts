import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "dev.openroadscope.app",
  appName: "OpenRoadScope",
  webDir: "dist-web",
};

export default config;
